const accountName = "ACCOUNT_NAME"
const accountPassword = "ACCOUNT_PASSWORD"
const accountKeyFile = ".../user.pem"
const clientKeyFile = ".../client.key"
const clientKeyPassphrase = "client.key.pass"
const clientCrtFile = ".../client.crt"
const caCrtFile = ".../ca.crt"
module.exports = {
    accountName,
    accountKeyFile,
    accountPassword,
    clientKeyFile,
    clientKeyPassphrase,
    clientCrtFile,
    caCrtFile
}